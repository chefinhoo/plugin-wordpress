{
  "name": "painel-administrativo-dra",
  "version": "1.0.0",
  "description": "Plugin WordPress com painel administrativo em React.",
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "build": "wp-scripts build src/index.js --output-path=assets/dist --output-filename=admin-app.js",
    "start": "wp-scripts start src/index.js --output-path=assets/dist --output-filename=admin-app.js"
  },
  "devDependencies": {
    "@wordpress/scripts": "^29.0.0"
  }
}
